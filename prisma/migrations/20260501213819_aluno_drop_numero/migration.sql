/*
  Warnings:

  - You are about to drop the column `numero` on the `Aluno` table.
    Numbering is now derived dynamically from alphabetical order of `nome` within a turma.

*/
-- AlterTable
ALTER TABLE "Aluno" DROP COLUMN "numero";
